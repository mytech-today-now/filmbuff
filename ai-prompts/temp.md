refactor filmbuff to use ai-powered (https://github.com/mytech-today-now/ai-powered.git) instead of ai from any other source.  Remove the ai provider abstraction and use ai-powered directly.  The only ai provider we want to support is ai-powered.  The user will have to install and run ai-powered themselves.  It should not be the responsibility of filmbuff to run ai-powered.  Filmbuff should simply use the ai-powered api to interact with the local ai-powered instance.  The user will have to set the AI_POWERED_URL environment variable to point to their local ai-powered instance.

ai-powered is available at https://github.com/mytech-today-now/ai-powered.  It is a local AI server that provides access to multiple AI models and providers.  It is designed to be run locally and is not intended to be used as a public service.  It is intended to be run on the same machine as filmbuff and accessed via http://localhost:3001 or a custom url set by the user.  filmbuff should have a flag to set the ai-powered url in case the user has set a custom url.  The default should be http://localhost:3001.  If the connection fails, filmbuff should prompt the user to ensure that ai-powered is running and accessible, and to check their connection and firewall settings.  The error message should include the correct url to access the ai-powered instance.

The ai-powered server provides a health check endpoint at /health.  Filmbuff should use this endpoint to verify that the ai-powered server is running and accessible before proceeding with any ai-powered requests.  The health check endpoint returns a 200 status code if the server is running and accessible.  If the server is not running or accessible, the health check endpoint returns a 404 status code.  Filmbuff should retry the health check after a short delay and fail permanently after 3 attempts.  The retry delay should double with each attempt (1s, 2s, 4s).  All of this logic should be handled by a single function that is used by the ai-powered client to ensure that the server is running and accessible before proceeding with any requests.

The ai-powered server provides a /v1/chat/completions endpoint that is used to generate text completions.  The ai-powered client should use this endpoint to generate text completions.  The /v1/chat/completions endpoint uses the same request and response format as the openai /v1/chat/completions endpoint.  The ai-powered client should be able to accept an optional ai-powered url flag that defaults to http://localhost:3001.  The ai-powered client should also accept an optional ai-powered model flag that defaults to gpt-4.  The ai-powered client should allow the user to specify a custom model for each request.  The ai-powered client should also allow the user to specify a custom system prompt for each request.  The ai-powered client should use the ai-powered model and system prompt for all requests unless otherwise specified.  The ai-powered client should also allow the user to specify a custom temperature for each request.  The ai-powered client should use the ai-powered temperature for all requests unless otherwise specified.  The ai-powered client should also allow the user to specify a custom max tokens for each request.  The ai-powered client should use the ai-powered max tokens for all requests unless otherwise specified.  The ai-powered client should also allow the user to specify a custom timeout for each request.  The ai-powered client should use the ai-powered timeout for all requests unless otherwise specified.  The ai-powered client should also allow the user to specify a custom retry count for each request.  

The ai-powered client should provide a default system prompt that explains the rules of the ai-powered server.  The default system prompt should be: "You are an AI assistant that helps people with their tasks.  You are not a general AI assistant like ChatGPT.  You are a custom AI assistant that is trained to help with specific tasks.  You will be provided with a task and you will provide a response.  Your response should be in Markdown format."

The ai-powered client should use the default system prompt unless the user specifies a custom system prompt.  The ai-powered client should allow the user to specify a custom system prompt for each request using a flag.  The ai-powered client should also allow the user to specify a custom system prompt to be used for all requests by setting an environment variable.  The ai-powered client should use the environment variable custom system prompt by default, but allow the user to override it with a flag.  The ai-powered client should also allow the user to specify a custom system prompt in the ai-powered config file.  The ai-powered client should use the config file custom system prompt by default, but allow the user to override it with an environment variable or a flag.  The config file should be located at ~/.ai-powered/config.json and should be a json file with a top level object that contains a "systemPrompt" property.  The config file should be optional and only used if it exists.  If the config file does not exist, the ai-powered client should use the default system prompt.  If the config file exists but does not contain a "systemPrompt" property, the ai-powered client should use the default system prompt.  If the config file contains a "systemPrompt" property, the ai-powered client should use the config file custom system prompt by default.

It is possible that the user will want to use a different ai-powered server for different requests.  For example, the user may want to use a different ai-powered server for different projects.  The ai-powered client should allow the user to specify a custom ai-powered url for each request using a flag.  The ai-powered client should also allow the user to specify a custom ai-powered url to be used for all requests by setting an environment variable.  The ai-powered client should use the environment variable custom ai-powered url by default, but allow the user to override it with a flag.  The ai-powered client should also allow the user to specify a custom ai-powered url in the ai-powered config file.  The ai-powered client should use the config file custom ai-powered url by default, but allow the user to override it with an environment variable or a flag.

The ai-powered client should allow the user to specify a custom ai-powered model for each request using a flag.  The ai-powered client should also allow the user to specify a custom ai-powered model to be used for all requests by setting an environment variable.  The ai-powered client should use the environment variable custom ai-powered model by default, but allow the user to override it with a flag.  The ai-powered client should also allow the user to specify a custom ai-powered model in the ai-powered config file.  The ai-powered client should use the config file custom ai-powered model by default, but allow the user to override it with an environment variable or a flag.

The ai-powered client should allow the user to specify a custom ai-powered temperature for each request using a flag.  The ai-powered client should also allow the user to specify a custom ai-powered temperature to be used for all requests by setting an environment variable.  The ai-powered client should use the environment variable custom ai-powered temperature by default, but allow the user to override it with a flag.  The ai-powered client should also allow the user to specify a custom ai-powered temperature in the ai-powered config file.  The ai-powered client should use the config file custom ai-powered temperature by default, but allow the user to override it with an environment variable or a flag.

The ai-powered client should allow the user to specify a custom ai-powered max tokens for each request using a flag.  The ai-powered client should also allow the user to specify a custom ai-powered max tokens to be used for all requests by setting an environment variable.  The ai-powered client should use the environment variable custom ai-powered max tokens by default, but allow the user to override it with a flag.  The ai-powered client should also allow the user to specify a custom ai-powered max tokens in the ai-powered config file.  The ai-powered client should use the config file custom ai-powered max tokens by default, but allow the user to override it with an environment variable or a flag.

The ai-powered client should allow the user to specify a custom ai-powered timeout for each request using a flag.  The ai-powered client should also allow the user to specify a custom ai-powered timeout to be used for all requests by setting an environment variable.  The ai-powered client should use the environment variable custom ai-powered timeout by default, but allow the user to override it with a flag.  The ai-powered client should also allow the user to specify a custom ai-powered timeout in the ai-powered config file.  The ai-powered client should use the config file custom ai-powered timeout by default, but allow the user to override it with an environment variable or a flag.

An althernative to the ai-powered client is to use the ai-powered server as a proxy for the openai api.  The ai-powered server provides a /v1/openai/completions endpoint that is a proxy for the openai /v1/completions endpoint.  The ai-powered server will automatically detect the correct model to use based on the request.  It will also automatically detect the correct api key to use based on the request.  The ai-powered server will automatically retry requests that fail due to rate limiting.  The ai-powered server will also automatically retry requests that fail due to connection issues.  The ai-powered server will automatically retry requests that fail due to server errors.  The ai-powered server will automatically retry requests that fail due to client errors.  The ai-powered server will automatically retry requests that fail due to timeouts.  The ai-powered server will automatically retry requests that fail due to any other reason.  The ai-powered server will automatically retry requests up to 3 times.  The ai-powered server will automatically retry requests with an exponential backoff.  The ai-powered server will automatically retry requests with a maximum delay of 10 seconds.  The ai-powered server will automatically retry requests with a minimum delay of 1 second.  The ai-powered server will automatically retry requests with a random delay between the minimum and maximum delay.
