# film-length Bead Tasks — Open / In-Progress
# Change: openspec/changes/film-length/ | JIRA: FB-NAR-1 | Priority: P1 all | Story: 21 pts
# Format: [ ID ] [title] [status] [blockers] [dependencies] [description]
# Sorted: highest priority → lowest; unblocked → blocked

[ bd-10da ] [ [FB-NAR-1] film-length: Narrative Length Selection & End-to-End Runtime Constraint ] [ open ] [ blockers: none ] [ blocks: bd-8573 ] [ EPIC. Six-domain runtime constraint system that gives filmbuff end-to-end runtime awareness. Gap 1: AI content-gen calls have no length constraint (30s commercial indistinguishable from 90m feature). Gap 2: generate-shot-list has no total-budget guard (44m drama silently produces 54m shot list). Gap 3: every AI call issued without runtime metadata. Solution — 6 spec domains: (1) 37-entry narrative length catalog across 7 categories, 6s–18000s compile-time constant; (2) Wizard Step 3A select dropdown between Slug and Tone, TOTAL_STEPS 11→12; (3) CLI flags --target-duration and --narrative-format on filmbuff start plus filmbuff narrative-lengths discovery command; (4) DB migration 002_add_target_duration.sql adding target_duration_seconds INTEGER and narrative_format_id TEXT, idempotent runner; (5) GeneratorConfig.totalBudgetSeconds budget enforcement — over-budget and under-budget warnings in shotList.warnings; (6) ## Target Runtime Constraint section injected into every AI system prompt when target_duration_seconds non-null. NEW files: narrative-length-catalog.ts, 002_add_target_duration.sql, runtime-constraint.ts. MODIFIED: start-wizard.ts, wizard-utils.ts, cli.ts, start.ts, types.ts, project-repository.ts, generator/types.ts, generate-shot-list.ts, generator/index.ts. AC-1–AC-24. Dependencies: @inquirer/prompts, 001_*.sql predecessor, FB-0042 (downstream), FB-DUR-1 (parallel). ]

[ bd-8573 ] [ [film-length] Phase 2: Catalog Module (narrative-length-catalog.ts) ] [ open ] [ blockers: bd-10da (Epic) ] [ blocks: bd-5a4d, bd-35ea, bd-5a09, bd-9576 ] [ Workstream 1 — single source of truth for all runtime presets; unblocks every other phase. Create cli/src/utils/narrative-length-catalog.ts. Define NarrativeLengthEntry { id, label, seconds, category, pageCountTarget, typicalGenres? } and NarrativeCategory union (7 values: ads-commercials, social-web, short-film, tv-episode, tv-movie, feature-film, epic-extended). Export NARRATIVE_LENGTH_CATALOG — 37 compile-time constant entries (Ads 6, Social/Web 7, Short Film 6, TV Episode 8, TV Movie 3, Feature Film 9, Epic/Extended 4), range 6s–18000s. Validation invariants: every id unique, every seconds a positive integer, every pageCountTarget === Math.round(seconds/60). Export findNarrativeLength(id) — O(n) lookup, returns undefined for unknown ids. Export formatRuntime(seconds) — M:SS (<3600s) or H:MM:SS (≥3600s). Export computeThreeActBudget(totalSeconds) — Syd Field 25/50/25, rounding absorbed into Act 2, sum === totalSeconds invariant. Design invariant: compile-time constant only, zero deps on other app modules, pageCountTarget NEVER stored in DB or payloads. Write unit tests (found+not-found, formatRuntime edge cases, sum invariant for all 37 entries). ≥95% branch coverage. Quick ref: findNarrativeLength('feature-90m')→{seconds:5400}; formatRuntime(5400)→"1:30:00"; computeThreeActBudget(5400)→{actOne:1350,actTwo:2700,actThree:1350}. Files: narrative-length-catalog.ts, __tests__/narrative-length-catalog.test.ts. Test plan: tests/narrative-length-catalog.test-plan.md. AC-2, AC-15, AC-16. ]

[ bd-5a4d ] [ [film-length] Phase 3: Database Migration (002_add_target_duration.sql) ] [ open ] [ blockers: bd-8573 (Phase 2) ] [ blocks: bd-5a09, bd-9576 ] [ Workstream 2. Create cli/src/db/migrations/002_add_target_duration.sql: ALTER TABLE projects ADD COLUMN target_duration_seconds INTEGER CHECK (target_duration_seconds IS NULL OR target_duration_seconds > 0); ALTER TABLE projects ADD COLUMN narrative_format_id TEXT. Update migration runner to wrap each ALTER TABLE in column-existence check (PRAGMA table_info or catch 'SQLITE_ERROR: duplicate column name' as no-op) for idempotency — SQLite lacks IF NOT EXISTS for ADD COLUMN. Extend Project interface: target_duration_seconds: number|null; narrative_format_id: string|null. Extend CreateProjectInput: target_duration_seconds?: number; narrative_format_id?: string. Update INSERT_PROJECT SQL string to include both new columns. Update create() binding: input.target_duration_seconds??null, input.narrative_format_id??null. Write migration idempotency test (run twice, assert no error on second run). Verify Number.isInteger(row.target_duration_seconds) for all non-null DB values. All existing ProjectRepository tests pass without modification. Security: SQLite INTEGER affinity truncates floats on insert. Files: 002_add_target_duration.sql, types.ts, project-repository.ts. Test plan: tests/database-migration.test-plan.md. AC-17, AC-18. ]

[ bd-35ea ] [ [film-length] Phase 4: Wizard Step 3A (stepNarrativeLength; TOTAL_STEPS 11→12) ] [ open ] [ blockers: bd-8573 (Phase 2) ] [ blocks: bd-9fc4 ] [ Workstream 3 — parallel with Phase 5. Extend WizardState (wizard-utils.ts): narrativeFormatId?: string; targetDurationSeconds?: number. Update buildEquivalentCommand() to append --target-duration and --narrative-format when set. Update stateToStartOptions() to forward both new fields. Implement stepNarrativeLength(prefill?,genre?) in start-wizard.ts: build choices array with Separator headers in category order (Ads & Commercials → Social & Web → Short Film → TV Episode → TV Movie → Feature Film → Epic & Extended → (skip/not set)); call select({choices, pageSize:20}) from @inquirer/prompts; print dim post-selection feedback "→ Pages target: ~90 pages  Act 1: 22:30 · Act 2: 45:00 · Act 3: 22:30"; print dim genre-coherence hint "ℹ Note: ..." when typicalGenres exists and excludes project genre — NEVER blocks advancement; return {narrativeFormatId, targetDurationSeconds} or both undefined on skip; re-entry pre-fills prefill as default. Insert step between slug and tone steps. Update TOTAL_STEPS 11→12 (all step-number display strings MUST update atomically — risk of regression). Update renderSummaryTable with three Runtime target display cases: format+duration "Runtime target: 90-minute comedy / horror  (1:30:00 / ~90p)", duration-only "Runtime target: 1:50:00", neither "(not set)" (dim). Write UT-WIZ-1–6. All existing wizard tests pass. Files: wizard-utils.ts, start-wizard.ts. Test plan: tests/wizard-step.test-plan.md. AC-1, AC-2, AC-3, AC-4, AC-5, AC-6, AC-7, AC-8, AC-9. ]

[ bd-5a09 ] [ [film-length] Phase 5: CLI Flags (--target-duration, --narrative-format, narrative-lengths) ] [ open ] [ blockers: bd-8573 (Phase 2), bd-5a4d (Phase 3) ] [ blocks: bd-9fc4 ] [ Workstream 4 — parallel with Phase 4. Extend StartOptions (start.ts): targetDurationSeconds?: number; narrativeFormatId?: string. Update startCommand() to forward both fields to projectRepo.create(). Register --target-duration <seconds> on filmbuff start (cli.ts): parser parseInt(v,10), throw commander error if isNaN(n)||n<=0 — --target-duration 0 and negative MUST throw before action handler. Register --narrative-format <id> on filmbuff start (cli.ts). Add flag resolution block in start action handler: (a) format-only → look up catalog → targetDuration=entry.seconds, exit 1 on unknown id with error referencing filmbuff narrative-lengths; (b) both supplied with mismatch → yellow warning, --target-duration wins (authoritative), exit 0; (c) both supplied matching → no warning, exit 0. Implement filmbuff narrative-lengths command — formatted table (id, label, formatted runtime) grouped by category, exit 0. Implement --json flag — full JSON array of 37 entries, exit 0. Write UT-CLI-1–8. Examples: filmbuff start --title "My Film" --genre horror --narrative-format "feature-90m"; filmbuff start --title "My Film" --genre thriller --target-duration 6600; filmbuff narrative-lengths; filmbuff narrative-lengths --json | jq '.[]|select(.category=="feature-film")'. Files: start.ts, cli.ts. Test plan: tests/cli-flags.test-plan.md. AC-9, AC-10, AC-11, AC-12, AC-13, AC-14, AC-15, AC-16. ]

[ bd-9576 ] [ [film-length] Phase 6: Duration Propagation & AI Prompt Injection (Workstream 5) ] [ open ] [ blockers: bd-8573 (Phase 2), bd-5a4d (Phase 3) ] [ blocks: bd-9fc4 ] [ Workstream 5. Extend GeneratorConfig (generator/types.ts): totalBudgetSeconds?: number; narrativeFormatLabel?: string. Update generate-shot-list.ts to resolve totalBudgetSeconds (priority: (1) explicit --target-duration CLI flag → (2) project DB target_duration_seconds → (3) undefined) and resolve narrativeFormatLabel via findNarrativeLength(project.narrative_format_id)?.label. Add budget enforcement block in generator/index.ts AFTER shotList.totalDuration is finalized: over-budget (totalSeconds > budget) → append {type:'duration-budget-exceeded',message} to shotList.warnings + yellow console.warn "⚠ Total shot duration 54:00 exceeds target runtime 44:00 by 10:00. Consider reducing scene count or shot lengths."; under-budget (totalSeconds < budget*0.80) → append {type:'duration-budget-under',message} to shotList.warnings + yellow console.warn; on-budget [budget*0.80, budget] → NO warning. Implement injectRuntimeConstraint(systemPrompt, project: Pick<Project,'target_duration_seconds'|'narrative_format_id'>) helper in runtime-constraint.ts — pure and stateless; when non-null appends "## Target Runtime Constraint\nThis project must run exactly **<label>** (<runtime> / ~<pageTarget> pages).\nThis is a hard constraint.\nThree-act structure budget (Syd Field paradigm):\n- Act 1: ~<actOne> (~<pages> pages)\n- Act 2: ~<actTwo> (~<pages> pages)\n- Act 3: ~<actThree> (~<pages> pages)"; when NULL returns systemPrompt unchanged. Call injectRuntimeConstraint in ALL AI system-prompt builders: logline, beat-sheet, screenplay, shot-list prep. ≥90% branch coverage on budget enforcement; ≥95% on injectRuntimeConstraint. Budget enforcement is GENERATOR-LEVEL only — CLI never blocks project creation. Files: generator/types.ts, generate-shot-list.ts, generator/index.ts, runtime-constraint.ts, all AI prompt builders. Test plans: tests/duration-propagation.test-plan.md, tests/ai-prompt-injection.test-plan.md. AC-19, AC-20, AC-21, AC-22, AC-23, AC-24. ]

[ bd-9fc4 ] [ [film-length] Phase 7: CI and Acceptance Verification (AC-1 through AC-24) ] [ open ] [ blockers: bd-35ea (Phase 4), bd-5a09 (Phase 5), bd-9576 (Phase 6) ] [ blocks: none ] [ Final gate. Confirm all 24 ACs pass: AC-1 Wizard Step 4/12 (after Slug before Tone); AC-2 all 37 entries grouped/ordered correctly; AC-3 skip leaves both fields undefined; AC-4 re-entry pre-fills; AC-5 dim post-selection feedback (pages + three-act); AC-6 genre-coherence hint fires when typicalGenres excludes genre, never blocks; AC-7 summary panel label+runtime+pages when set from catalog; AC-8 summary panel "(not set)" (dim) when no runtime; AC-9 buildEquivalentCommand includes both flags; AC-10 --target-duration 5400 stores 5400, narrative_format_id=NULL; AC-11 --narrative-format feature-90m derives 5400, stores both; AC-12 unknown --narrative-format exits 1 with clear error; AC-13 --target-duration 0 or negative throws commander error; AC-14 mismatch yellow warning, --target-duration wins, exit 0; AC-15 filmbuff narrative-lengths table exit 0; AC-16 narrative-lengths --json parseable exit 0; AC-17 002_add_target_duration.sql idempotent (runs twice no error); AC-18 target_duration_seconds INTEGER Number.isInteger true; AC-19 generate-shot-list reads DB → totalBudgetSeconds; AC-20 over-budget duration-budget-exceeded in warnings; AC-21 under-budget(<80%) duration-budget-under in warnings; AC-22 on-budget([80%,100%]) no warning; AC-23 every AI prompt has ## Target Runtime Constraint when non-null; AC-24 null → no section injected no error. Confirm all existing wizard + ProjectRepository tests pass (no regression). CI green. PR reviewed, approved, merge. Exit conditions: catalog ≥95% branch coverage, budget enforcement ≥90% branch coverage, DB idempotency test passes. Source: openspec/changes/film-length/summary.md, jira/FB-NAR-1.md. ]
